# Historical finding verification

The original [review](../../effect-rfc-review.md) contains 73 findings against an
older worktree. This ledger tracks their verification against the current code.
`Pending` is not an assertion that the old defect remains; it means the original
acceptance criterion has not yet been re-audited here.

| Finding    | Original issue                                                                 | Current audit status | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ---------- | ------------------------------------------------------------------------------ | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| EFFECT-001 | O runner e incompativel com `@effect/vitest` e nenhuma suite executa           | Verified fixed       | Workspace catalog pins Vitest 4.1.5 and @effect/vitest 4.0.0-rc.112; current full run executes 1,610 core, 87 CLI, and 104 website tests                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| EFFECT-002 | O Layer live de `LocalStorage` esta ligado ao backend de sessao                | Verified fixed       | platform/storage.ts wires localStorageBrowser to globalThis.localStorage, separately from sessionStorageBrowser; current storage adapter suite passes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| EFFECT-003 | Registro de listeners e observers nao e atomico com o finalizer                | Verified fixed       | Native registrations use acquireRelease; registration-interruption.test.ts interrupts inside addEventListener/requestIdleCallback/IntersectionObserver construction/MutationObserver.observe and proves one release. CLI UI test interrupts Signal listener insertion and verifies zero retained subscriptions                                                                                                                                                                                                                                                                                                                                         |
| EFFECT-004 | Falhas nativas esperadas sao declaradas como impossiveis e viram defects       | Verified fixed       | EventTarget/Idle/Observer expose project-owned error channels and native try boundaries; browser error matrices cover construction, registration, dispatch, observe/unobserve, unsupported APIs, and release reporting while other finalizers complete                                                                                                                                                                                                                                                                                                                                                                                                 |
| EFFECT-005 | Callbacks podem escapar do runtime e suas falhas nao sao observadas            | Verified fixed       | EventTarget, Idle, and both Observer callbacks construct work inside Effect.suspend, attach it to Scope before execution, and observe terminal failure. Native/controlled tests cover construction throws, later Fail/Die, exactly one report, silent owner interruption, and awaited cleanup. Actual incident shortcut and media implementations additionally cover construction/execution/read failures and blocked callback finalization. Telemetry value projection remains a separate RFC 17.4 audit                                                                                                                                              |
| EFFECT-006 | Operacoes do adapter DOM podem reportar sucesso ou consultar fora do boundary  | Verified fixed       | platform/dom.ts rejects false Reflect.set, uses the supplied query root directly, and checks required document elements; dom.test.ts exercises native rejected writes, invalid roots, and missing structural elements                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| EFFECT-007 | Fibers de rerender nao sao registradas no Scope de render                      | Verified fixed       | runOwnedRenderFiber registers via forkIn before flushing; component rerender uses componentRuntime.scope. render-lifecycle-regressions.test.ts blocks a real rerender, closes its mount, and proves interruption without resurrected DOM                                                                                                                                                                                                                                                                                                                                                                                                               |
| EFFECT-008 | Event handlers pertencem ao mount, nao ao subtree que fornece seus Services    | Verified fixed       | Component and keyed-row render contexts carry local Scopes; both intrinsic event paths use suspended thunks through runOwnedRenderFiber/forkIn. Removing a provided Component keeps its provider alive until blocked handler cleanup completes; the root survives. Existing row-removal and window.error containment tests pass. A new 1,001-event regression observes constant finalizer counts in the actual handler owner and mount after completed callbacks                                                                                                                                                                                       |
| EFFECT-009 | O merge de Context faz o root sobrescrever providers locais                    | Verified fixed       | renderer mergeRenderServices gives captured local Context precedence and preserves structural Scope. render-lifecycle-regressions.test.ts verifies local body/handler/rerender/cleanup with isolated root sibling                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| EFFECT-010 | A pipeline de render transforma interrupcao em sucesso ou fallback             | Verified fixed       | Replace/reconcile preserve Die and Interrupt Reasons; only typed render failure yields FailedBeforeCommit and callers re-emit its Cause. ErrorBoundary refuses pure/mixed defect or interruption fallback and closes children with their real Exits. New rollback tests reproduced successful staged-scope closure after typed failure and loss of the render Cause when release defects; RenderTransaction now closes with the original failure and combines rollback failure. Public boundary tests verify typed fallback plus terminal mixed Causes in render and finalizer Exits                                                                   |
| EFFECT-011 | O commit pode perder o unico owner do novo subtree                             | Verified fixed       | RenderTransaction.replace retains staged ownership until onCommit and returns committed result despite prior cleanup failure; tests inject failed DOM insert plus failed rollback and verify next disposal after failed prior cleanup                                                                                                                                                                                                                                                                                                                                                                                                                  |
| EFFECT-012 | Portal e document mount vazam mutacoes quando a construcao falha               | Verified fixed       | Portal stages children and closes its scope/results on failure; document renderer rolls back mutations. Production portal and document rollback tests fail the second child and verify unchanged target/document plus acquired child finalization                                                                                                                                                                                                                                                                                                                                                                                                      |
| EFFECT-013 | Navegacoes concorrentes publicam estado rasgado e permitem stale overwrite     | Verified fixed       | NavigationCore serializes adapter mutation and snapshot; Router publishes a monotonically versioned Signal with query projected from the same cell. Deferred tests cover a slow adapter and slow listener, finish both callers, and require winner path/query/version. NAVIGATION-001 additionally protects reconciliation and hands committed publication to the Router scope. Public tests cover browser cancellation, reentrant navigation, awaited Layer cleanup, and preservation and projected reporting of mixed terminal Causes                                                                                                                |
| EFFECT-014 | Ativacao stale nao e interrompida e pode trocar DOM ou aplicar scroll          | Verified fixed       | RouteActivation owns supersession/quiescence tokens and awaits predecessor finalization before visible replacement. Deferred tests interrupt/finalize lazy load, loading fallback, swap, and scroll; real Outlet tests require exact Element insertion acknowledgment and winner-only scroll/commit. Requests carry their version and ScrollIntent. NAVIGATION-002 additionally makes built-in Outlet identity admission monotonic and constant-size, including rejection of skipped older versions                                                                                                                                                    |
| EFFECT-015 | O Outlet mantem um segundo matcher que diverge do contrato canonico            | Verified fixed       | The independent trie is removed: Outlet and prefetch use RouteMatcher.fromResolved, the same compiled matcher as RouteMatcher.make. Tests preserve sibling dynamic parameter names and malformed URI failures; shared matching/path-pattern suites cover static/param/wildcard priority, zero/required catch-all, and rejection of non-terminal wildcard/catch-all suffixes                                                                                                                                                                                                                                                                            |
| EFFECT-016 | Trace HTTP inclui a query completa no evento de request                        | Verified fixed       | Real Node/Bun adapter tests require method/pathname and exclude query sentinels from recorder JSON/Markdown. Generated factory tests additionally intercept attributes before tracer delivery, covering sampled/unsampled success, failure, defect, interruption, and mixed Cause; actual Node/Bun production process probes retain transport data and traceparent while excluding query/header/response sentinels from spans. Automatic HTTP logs also project Causes before logger delivery                                                                                                                                                          |
| EFFECT-017 | Falha da instrumentacao pode interromper uma mutacao ja commitada              | Verified fixed       | Trace contains payload and logger defects without retry. trace.test.ts verifies committed Signal value, listener notification, metric increment, and hostile value identity; new tests request actual fiber interruption from payload construction and logger delivery and retain the interrupted Exit with no following business step or duplicate emission                                                                                                                                                                                                                                                                                           |
| EFFECT-018 | Habilitar Trace muda a execucao de `Signal.derive`                             | Verified fixed       | Signal.derive computes initialValue once, stores it, and describes that same value in signal.derive.create. signal.test.ts runs a counted projection that throws on a second invocation with Trace.record and with Fatal filtering; both return one call and value 6. Trace.record's actual implementation lowers MinimumLogLevel to Trace, including the cost event, so the enabled case exercises payload construction                                                                                                                                                                                                                               |
| EFFECT-019 | Nome do evento e tipado, mas fatos e origem do Trace nao sao                   | Verified fixed       | Event schemas correlate name and exact facts. trace-types.test.ts rejects missing/extra/wrong-typed fields, including inferred variables; the complete typecheck consumes those expectations. Runtime tests reject homonymous Effect.log, malformed/excessive facts, copied envelopes, and late replay; only marked, decoded events reach registered readers                                                                                                                                                                                                                                                                                           |
| EFFECT-020 | `Trace.withAction` pode emitir start sem terminal e chama interrupcao de falha | Verified fixed       | withAction protects start/terminal with uninterruptibleMask and restores the operation; onExit emits one cause-free completed/failed/interrupted terminal. Explicit and Deferred-coordinated external interruption tests retain the interrupted Exit; eight pure/mixed Cause cases preserve the original Cause                                                                                                                                                                                                                                                                                                                                         |
| EFFECT-021 | Recorder preserva referencias mutaveis e nao garante JSON serializavel         | Verified fixed       | emitInternal decodes then detaches before recording. An actual withAction test mutates nested object/array facts during and after the action, then checks recorder, snapshot, JSON and Markdown history. Direct detachment tests cover cycles, BigInt, getters/toJSON, revoked Proxy, and bounded output; recorder/report tests verify freezing and JSON serialization                                                                                                                                                                                                                                                                                 |
| EFFECT-022 | `Debug.layer` preserva outro Debug e duplica eventos/filtros                   | Verified fixed       | Debug marks its loggers in a WeakSet and replaces only Debug/default loggers. Actual nested-layer tests require one console line, suppress a router event rejected by the inner signal filter, and retain independent recorder delivery                                                                                                                                                                                                                                                                                                                                                                                                                |
| EFFECT-023 | O signal compartilhado do cache pertence ao primeiro componente consumidor     | Verified fixed       | makeEntry creates entry.scope before Signal.makeInScope, bypassing component slots and owner References. Shared-entry test uses two component scopes/phases, closes the first, invalidates, and proves the second updates with no disposed-access event; per-render reactive test also preserves output identity                                                                                                                                                                                                                                                                                                                                       |
| EFFECT-024 | `Resource.hash` usa um hash de 32 bits como identidade unica                   | Verified fixed       | v3 length-framed positional primitive encoding distinguishes absence, undefined, null, types, special numbers, prefixes and delimiters. Opaque objects/functions use shared weak reference identity across HMR copies; collision, Proxy/getter, cyclic/function, and Symbol tests cover the documented identity model                                                                                                                                                                                                                                                                                                                                  |
| EFFECT-025 | O claim single-flight e uma sequencia read-then-set nao atomica                | Verified fixed       | All four fetch/invalidate/refresh/reactive paths use claimFlight with atomic Ref.modify and identity-checked completion. Parallel production-API tests prove one worker, shared settlement, and cleared flight refs. resource-owner.test.ts additionally proves reentrant clear awaits the worker release                                                                                                                                                                                                                                                                                                                                              |
| EFFECT-026 | Cada rerender acumula subscriptions e daemons de `fetchReactive`               | Verified fixed       | fetchReactive selects CurrentRenderScope for daemon/subscription lifetime while Signal.make preserves component output identity. Twelve replacement renders retain one parameter and one entry listener; one key change fetches once and render close removes both. Reentrant daemon shutdown and cached-return ordering now have dedicated tests                                                                                                                                                                                                                                                                                                      |
| EFFECT-027 | Resource converte defects e Causes mistos em `Failure<E>`                      | Verified fixed       | runFetch publishes Failure only when every Cause reason is Fail and documents first-reason selection. Lone-owner and shared-waiter tests cover typed failure, pure defect/interruption, and mixed Causes, exact terminal reporting, identity retirement, and fresh retry                                                                                                                                                                                                                                                                                                                                                                               |
| EFFECT-028 | O registry nao possui capacidade, TTL ou eviction                              | Verified fixed       | Layer policy defaults to capacity 256 and lazy 30-minute idle TTL. Tests cover LRU disposal, exact TestClock boundaries, protected in-flight leases, retired generations, reserved candidates, interrupted batches, saturated admission, clear, and recreation. New expiration tests preserve renewal and backward-clock behavior                                                                                                                                                                                                                                                                                                                      |
| EFFECT-029 | `clear` fecha Scope enquanto segura o lock global do cache                     | Verified fixed       | Registry Ref callbacks remove/retire entries synchronously; closeEntries runs afterward and releases capacity in ensuring. A blocked A finalizer permits A recreation and B work before release; cleanup regressions preserve failures and release every retired slot                                                                                                                                                                                                                                                                                                                                                                                  |
| EFFECT-030 | `SafeUrl` aceita formas canonicas de `javascript:`                             | Verified fixed       | SafeUrl parses with WHATWG URL before deciding scheme policy, rejects ASCII controls, and checks normalized protocol. Tests reject case/space/CR/LF/tab/NUL disguises, reject malformed hosts, preserve percent-encoded relative paths, and preserve base-dependent references without substituting the validation sentinel                                                                                                                                                                                                                                                                                                                            |
| EFFECT-031 | Uma allowlist global de URL e aplicada a sinks com riscos diferentes           | Verified fixed       | render-utils owns element/attribute policies for single URLs, srcset candidates, and space-separated lists. Renderer tests cover href/src/action/formAction/srcSet plus object data, cite, poster, ping, and generic fallback; hostile srcset delimiter/parenthesis/data-comma cases are rejected while approved image/media and custom navigation policies remain scoped                                                                                                                                                                                                                                                                              |
| EFFECT-032 | Params de rota nao possuem codec URI de ida e volta                            | Verified fixed       | path-pattern encodes each simple parameter as one URI segment, encodes catch-all segments separately, and decodes pathname segments once before matching/Schema. Tests round-trip reserved characters, slash, percent, Unicode, and catch-all paths; reject dot segments and malformed encoding with typed errors; matching-performance verifies one decode per input segment across 4000 candidates                                                                                                                                                                                                                                                   |
| EFFECT-033 | O output decodificado da Schema de params e convertido de volta para string    | Verified fixed       | CurrentRouteParams retains decoded objects by active route pattern. Routed Component.gen tests observe number:123 from NumberFromString, preserve separately decoded ancestor/leaf values, and reject requests outside the active match chain with RouteParamsPatternMismatch                                                                                                                                                                                                                                                                                                                                                                          |
| EFFECT-034 | Middleware reduz defect/interrupcao a resultado ordinario e perde a Cause      | Verified fixed       | MiddlewareResult.Error retains Cause, the classifier re-emits interruption, and Outlet forwards Cause to renderError. New middleware-boundary-cause.test.tsx exercises actual rendered Components for Fail/Die/mixed Causes, pure/mixed interruption, Forbidden, and direct/chained Redirect; emitted runtime annotations and payloads are preserved, with no protected-page or interruption fallback execution                                                                                                                                                                                                                                        |
| EFFECT-035 | O boundary JSX repara props hostis e produz Element parcial                    | Verified fixed       | runJsx no longer catches the synchronous build failure for a defensive retry. Both JSX builder paths abort on enumeration/getter failure; jsx/jsxDEV/jsxs/jsxsDEV regression tests retain the original defect and return no partial Element                                                                                                                                                                                                                                                                                                                                                                                                            |
| EFFECT-036 | O Worker Cloudflare escrito no build nao e o Worker exercitado pelos testes    | Verified fixed       | GeneratedArtifactPlanner is the only Worker renderer. generated-artifact-planner.test.ts and plugin.test.ts import the actual WriteFile operation.contents via a data module, then execute deep-route shell fallback, API/method/asset 404s, successful assets, malformed/encoded paths, and fallback through / without /index.html                                                                                                                                                                                                                                                                                                                    |
| EFFECT-037 | A API de desenvolvimento publica `Ready` mesmo quando a inicializacao falha    | Verified fixed       | PluginApi plus actual Node/Bun adapters cover import/composition/handler failures: Loading becomes Failed, never Ready, and partial handler resources close once. Six fresh Node/Bun subprocesses execute real configureServer, app/api.ts and the generated factory; actual import/invalid-Layer/acquisition failures reject startup, emit no API handlers loaded message, and release the acquisition fixture once. Mixed acquisition/cleanup Causes and closed-owner admission also have regression coverage                                                                                                                                        |
| EFFECT-038 | Reload descarta o handler saudavel antes de adquirir o substituto              | Verified fixed       | Both adapters acquire before an atomic guarded Ref swap and await old Scope.close. Six real-HTTP cases cover Node/Bun import/composition/handler reload failures, preserving generation one and recovering to generation three. Four SSR-generated handler tests cover both adapters with blocked handler/stream finalizers: generation two serves while reload waits, then request cleanup precedes old API Layer release exactly once. The handler cases first reproduced premature Layer release; the factory now owns actual HTTP fibers. Existing tests cover interruption before/after swap                                                      |
| EFFECT-039 | O lifecycle do Vite nao descarta o `ManagedRuntime` nem todos os API Scopes    | Verified fixed       | Shutdown memoizes one Promise, closes lifecycleScope, and ensures pluginRuntime.disposeEffect. Four real Vite tests cover normal/middleware mode through closeBundle/buildEnd with a blocked API Layer finalizer: concurrent hooks share the Promise, runtime-backed transform still works while cleanup is blocked, release runs once, and transform fails with ManagedRuntime disposed only after shutdown completes                                                                                                                                                                                                                                 |
| EFFECT-040 | As bridges HTTP aceitam body ilimitado e nao propagam desconexao               | Verified fixed       | Bun bridge tests and the actual SSR-generated Node bridge cover incremental 1 MiB limits, chunked overflow, aborted body reads, reader cancellation/lock release, listener removal, and distinct cookies. Eight generated Node/Bun handler/stream tests verify native TCP disconnect and active-request replacement cleanup. Three generated Web response tests cover disposal before reader acquisition, user body replacement, and HEAD; the factory owns streaming fibers and avoids transferring HEAD cleanup to a discarded stream                                                                                                                |
| EFFECT-041 | O checker ignora diagnosticos sem arquivo e erros semanticos do `tsconfig`     | Verified fixed       | checkProject collects config, options, global, syntactic, and semantic diagnostics; fileless diagnostics receive tsconfig coordinates before source filtering. checker.test.ts verifies TS6046, TS18003, and TS2318 in results and executes the real Bun CLI for those plus TS1109/TS2322, asserting JSON diagnostic codes and process exit 1. All cases pass in the current full suite                                                                                                                                                                                                                                                                |
| EFFECT-042 | Config e startup do servidor gerado escapam de Schema e readiness              | Verified fixed       | defineConfig validates the canonical Schema and throws TryggConfigError with the original decode cause before plugin runtime construction. Real Node/Bun artifacts reject PORT 0/65536/fractional/NaN/empty and empty HOST; PORT 1/4173/65535 reach the typed missing-shell boundary. Occupied ports fail with ServerStartupError. Every process exits 1 without readiness. Generated startup decodes config, reads the shell, and awaits Layer.build before logging listening                                                                                                                                                                         |
| EFFECT-043 | O watcher aceita Promise, mas o callback nativo nao possui seu resultado       | Verified fixed       | ViteServer watcher listeners return void and use the scoped callback runtime; plugin tests assert one terminal report with mixed Cause and awaited interruption. callback-owner.test.ts additionally proves reentrant shutdown waits for release, closed-owner admission is rejected, and captured Scheduler survives                                                                                                                                                                                                                                                                                                                                  |
| EFFECT-044 | Interromper o bootstrap deixa readiness pendente para sempre                   | Verified fixed       | Bootstrap.initialize atomically claims Pending and settles the shared Deferred from the complete Exit under uninterruptibleMask. Six bootstrap tests cover typed failure, defect, interruption, mixed Causes, interruption at initializer entry, and concurrent initialize/awaitReady callers. Canceling one waiter does not cancel initialization; failed initialization runs once and current/future callers retain its original failure                                                                                                                                                                                                             |
| EFFECT-045 | Erros de filesystem sao recuperados como "arquivo ausente"                     | Verified fixed       | PluginFiles tests inject NotFound, PermissionDenied, and TimedOut independently into exists, stat, and readFileString. Only NotFound recovers; other failures retain PluginFileSystemError and their operation. Source inspection confirms the generated server classifies only ENOENT as NotFound and falls through only for that reason                                                                                                                                                                                                                                                                                                              |
| EFFECT-046 | A bridge perde headers repetidos, inclusive `Set-Cookie`                       | Verified fixed       | Both the Bun bridge and SSR-generated Node bridge skip Set-Cookie in Headers.forEach and write Headers.getSetCookie() as an array. Production-helper tests verify two distinct cookies including an Expires comma, without folding or overwriting                                                                                                                                                                                                                                                                                                                                                                                                      |
| EFFECT-047 | O fallback HTML transforma qualquer defect/interrupcao em `next()`             | Verified fixed       | transformHtmlForFallback recovers only all-Fail Causes whose project error operation is transform, before touching the response. Production middleware tests cover typed next, response defect, interrupted transformation, and mixed-Cause preservation                                                                                                                                                                                                                                                                                                                                                                                               |
| EFFECT-048 | O codegen de rotas infere outputs de Schema por regex e inventa `string`       | Verified fixed       | PluginFiles calls generateRouteTypes, whose AST/TypeChecker resolves canonical Route identity and installed Schema.Struct Type/Encoded members. It rejects any/unknown, incompatible or unresolved schema shapes, and non-string URL encodings with PluginParseError. route-codegen.test.ts covers aliases, references, brands, Date/BigInt transforms, inline schemas, and rejected array encodings; actual generated declarations compile with Link/navigate consumers. Vite buildStart tests reject lookalikes and unresolved builder flow in transpile-only sources                                                                                |
| EFFECT-049 | `Signal.update` perde read-modify-writes concorrentes                          | Verified fixed       | Signal.update holds the per-signal semaphore across disposed check, read, projection, equality, and commit; notification occurs after releasing it. signal.test.ts releases 100 concurrent increments with MaxOpsBeforeYield 5 and gets exactly 100, and a listener reentering the same Signal reaches 2 without deadlock                                                                                                                                                                                                                                                                                                                              |
| EFFECT-050 | Derivacoes podem retornar snapshot stale adquirido antes da subscription       | Verified fixed       | derive/deriveAll/cx capture revisions before the initial projection, subscribe, then reconcile if revisions changed. makeVersionedReconciler validates revisions at publication. Five hostile acquisition tests force updates before subscription and again during reconciliation, including both deriveAll sources; returned values/classes already reflect the latest updates without sleeps                                                                                                                                                                                                                                                         |
| EFFECT-051 | `Signal.suspend` cria um Scope independente sem owner fora de Component        | Verified fixed       | Signal.suspend uses the component or ambient Scope directly. Tests block both initial work and a rerender after subscription, then verify interrupted work, awaited finalizers, listener removal, and disposed view. New regressions reject closed-owner admission and suppress Pending callbacks during blocked shutdown                                                                                                                                                                                                                                                                                                                              |
| EFFECT-052 | Boundaries reativos apagam failure, defect e interruption                      | Verified fixed       | Listener and keyed-row tests cover Fail/Die/Interrupt and mixed Causes; only all-Fail recovers. Suspend tests observe actual terminal worker Exits and complete all-Fail fallback Causes. Its documented source-interruption policy renders a terminal Failure view while preserving/reporting the interrupted worker Exit; owner cancellation renders no fallback, and defects/mixed Causes remain terminal                                                                                                                                                                                                                                           |
| EFFECT-053 | Keyed diff remove o DOM confirmado antes de validar o proximo snapshot         | Verified fixed       | renderKeyedList stages in a DocumentFragment before destructive commit and rolls back failed staging. Tests cover replacement, append, changed-row rerender, and combined remove/add/move failure, preserving old nodes and state, closing staging exactly once, reporting one Cause, and recovering on retry                                                                                                                                                                                                                                                                                                                                          |
| EFFECT-054 | Reorder preserva rows e indices stale                                          | Verified fixed       | needsRerender checks item identity OR changed index independently of moves. The regression replaces and reorders objects, then reverses those same objects again; labels and indices update while keyed text-node identities remain intact                                                                                                                                                                                                                                                                                                                                                                                                             |
| EFFECT-055 | Coalescing de SignalElement ocorre depois de criar Fibers sem limite           | Verified fixed       | SignalElement sets workerRunning before admission and coalesces updates into swapVersion/swapPending. A blocked-render test sends 1,000 updates and observes exactly one owned worker, only the blocked/latest renders, and final latest-999 DOM                                                                                                                                                                                                                                                                                                                                                                                                       |
| EFFECT-056 | Keys duplicadas deixam row e Scope sem owner                                   | Verified fixed       | Key uniqueness is validated before row staging/commit. Initial and update duplicate tests assert KeyedListDuplicateKeyError, zero duplicate render calls, preserved valid nodes, and exactly one finalization of each valid row                                                                                                                                                                                                                                                                                                                                                                                                                        |
| EFFECT-057 | Subscription pode ser instalada depois que o Signal foi disposto               | Verified fixed       | Subscription insertion and disposal share the signal gate; subscribeUnsafe checks disposed state synchronously. Tests queue subscribe-first and close-first behind a held gate, then require zero retained listeners and harmless late unsubscribe in both orderings                                                                                                                                                                                                                                                                                                                                                                                   |
| EFFECT-058 | O template publica mutacoes HTTP sem decisao de autorizacao                    | Verified fixed       | Default TokenMutationPolicyLive closes mutations without configuration. Native WebCrypto verifies a bounded bearer credential before providing the authenticated operator principal or acquiring the repository. Actual HTTP tests cover missing/malformed/wrong tokens, empty/invalid server configuration, canonical 401/403 errors, and a real web-handler Bearer challenge; successful requests use configured credentials. AppServicesLive provides the actual client middleware and tab-owned credential service; form/forget/closed-owner tests pass. The shared operator token is explicitly documented, not an individual-user or OIDC system |
| EFFECT-059 | O template `incident` exporta um grafo executavel incompleto                   | Verified fixed       | Root composition supplies ApiClient and AppTheme to layout and route wrappers; template typecheck and actual incident scaffold build pass. incident-template-ui.test.tsx renders Settings, changes the root html theme, and proves root/settings Signal identity, exactly one theme acquisition/native listener, and one removal on owner shutdown                                                                                                                                                                                                                                                                                                     |
| EFFECT-060 | Processos filhos do CLI nao preservam falha, interrupcao ou finalizacao        | Verified fixed       | runProcess uses ChildProcessSpawner with separate executable/args and typed spawn/status/exit failures. Controlled tests verify nonzero/spawn errors and cancellation waiting for group and spawner finalizers; the real POSIX descendant test lets the leader exit first and proves interruption still waits for descendant cleanup and PID disappearance. VCS advisory recovery is explicit; install failures remain terminal; unsupported group platforms fail before spawn                                                                                                                                                                         |
| EFFECT-061 | PR e publicacao nao verificam CLI nem templates                                | Verified fixed       | Root check includes CLI and template typechecks; root test includes the complete CLI suite and actual blank/incident scaffold typecheck/build. PR and both publication workflows require those gates. Release reruns CLI/template checks and scaffold smoke after dependency synchronization, then verifies source revision and smokes the exact packed artifact before publishing                                                                                                                                                                                                                                                                     |
| EFFECT-062 | Schemas HTTP duplicados perdem a identidade dos erros canonicos                | Verified fixed       | api.ts annotates imported IncidentNotFound/InvalidTransition classes; incident-template.test.ts exercises the production HTTP layers and verifies decoded client errors retain canonical class identity                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| EFFECT-063 | Schemas de entrada aceitam IDs e titulos invalidos                             | Verified fixed       | Shared branded IncidentId and trimmed 1–120-character IncidentTitle schemas feed HTTP params/payloads; boundary tests reject nonpositive, fractional, nonfinite, unsafe IDs and blank/oversized titles                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| EFFECT-064 | O estado do adapter `IncidentsLive` nasce fora da aquisicao do Layer           | Verified fixed       | Incidents.layer allocates its cached make Effect during Layer construction; makeMemory clones seed Dates and owns a fresh Map/counter. Tests prove one shared acquisition and separate state/Date identities across Layer acquisitions                                                                                                                                                                                                                                                                                                                                                                                                                 |
| EFFECT-065 | Scaffold falho ou interrompido deixa destino parcial e bloqueia retry          | Verified fixed       | Sibling staging and exclusive mkdir reservation preserve concurrent winners; publication failure and delayed rename interruption verify rollback. New SCAFFOLD-001 regressions reproduced cleanup racing directory/copy/write completion; each native staging mutation now settles before interruption cleanup, and all three cases prove successful retry. Eleven operational-failure cases cover app/public traversal, API directory/declarations, stylesheet/README, and every generated configuration file, preserving the exact PlatformError, removing owned paths, and permitting retry                                                         |
| EFFECT-066 | O CLI classifica falhas externas como cancelamento ou `unknown`                | Verified fixed       | Prompt adapter tests distinguish explicit cancellation, rejected Promise as PromptFailedError with original cause, and unknown selection as InvalidPromptResponseError. Scaffold failure tests preserve PlatformError object identity. copyDir and the CLI keep inferred typed error channels; the terminal root catches only PromptCancelledError, leaving operational failures to runMain                                                                                                                                                                                                                                                            |
| EFFECT-067 | Cookie e APIs de tema transformam input/browser failures em defects            | Verified fixed       | ThemeBrowser wraps cookie, media query, and listener host operations in typed try boundaries; stored preference uses Schema. incident-template.test.ts covers malformed cookie, invalid preference, getter/setter throws, missing media support, registration/removal failures, and exact cleanup; theme owner regression additionally proves awaited callback release                                                                                                                                                                                                                                                                                 |
| EFFECT-068 | Doubles redefinem os ports e nenhum teste exercita os adapters live            | Verified fixed       | DOM and Storage suites run shared public contract cases against browser and test Layers. DOM assertions inspect actual node mutation, properties/styles, selectors, and document identity. Storage installs independent native globals and checks both public values and selected native backend, so LocalStorage wired to sessionStorage fails the same case                                                                                                                                                                                                                                                                                          |
| EFFECT-069 | Scroll converte defects e interrupcao em `ignoredError`                        | Verified fixed       | router/service.ts applies all-reasons typed-only recovery at applyScrollForNavigation, used by browserLayer; scroll-recovery.test.ts covers typed failure, defect, and interruption for DOM, Storage, and Scroll                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| EFFECT-070 | Cleanup pode parar no primeiro release e apagar sua `Cause`                    | Verified fixed       | Fragment and Intrinsic route every release through cleanupAll; direct cleanup tests preserve defects and release siblings. Root mount cleanup now preserves defects and mixed interruption; mount-cleanup-cause.test.ts reproduces the former successful Exit and passes both variants                                                                                                                                                                                                                                                                                                                                                                 |
| EFFECT-071 | `type()` retorna antes dos handlers Effect que disparou                        | Verified fixed       | type and click share flushInteractionEffects. Real rendered onInput tests cross Effect.yieldNow and update reactive DOM before type returns; a separate Deferred-blocked handler proves type returns without waiting for external work, leaves DOM unchanged while blocked, and publishes the update after explicit release                                                                                                                                                                                                                                                                                                                            |
| EFFECT-072 | O teste de fechamento de Signal e tautologico                                  | Verified fixed       | The replacement test keeps the source Signal alive, registers unsubscribe on a separate Scope, closes it, updates the source, and requires zero calls. Omitting unsubscribe makes that assertion fail. Suspend ownership claims separately start real work and observe interruption plus blocked finalizer completion                                                                                                                                                                                                                                                                                                                                  |
| EFFECT-073 | O gate `check` falha antes de estabelecer a evidencia de lint                  | Verified fixed       | check executes lint, formatting, typecheck, benchmark typecheck, type fixtures, and Effect diagnostics; current complete check passes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
